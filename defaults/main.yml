---
vorta:
  user:
  config:
    init: false
    src: vorta.franzk.json.j2
    borg:
      pw:
  nas:
    borg_ssh_private_key_file: id_ed25519_borg-backup
    borg_ssh_public_key_file: id_ed25519_borg-backup.pub
  flatpak:
    state: present
    scope: system
  apt:
    state: absent
